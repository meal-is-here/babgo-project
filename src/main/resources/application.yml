spring:
  profiles:
    active: local
  config:
    import: optional:file:.env[.properties]
  application:
    name: babgo

logging:
  level:
    com.babgo: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG